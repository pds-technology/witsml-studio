<UserControl x:Class="PDS.WITSMLstudio.Desktop.Plugins.EtpBrowser.Views.Streaming12View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition />
            <ColumnDefinition Width="50"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="40" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" Grid.ColumnSpan="3" Margin="5,0,5,5" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="StartStreaming" Content="Start Streaming" Margin="0,0,5,0" Width="100" />
            <Button x:Name="StopStreaming" Content="Stop Streaming" Margin="0,0,5,0" Width="100" />
        </StackPanel>
    </Grid>
</UserControl>
